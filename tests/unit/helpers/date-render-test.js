import { dateRender } from 'ebookpull/helpers/date-render';
import { module, test } from 'qunit';

module('Unit | Helper | date render');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateRender([42]);
  assert.ok(result);
});
