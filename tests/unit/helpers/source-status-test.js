import { sourceStatus } from 'ebookpull/helpers/source-status';
import { module, test } from 'qunit';

module('Unit | Helper | source status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sourceStatus([42]);
  assert.ok(result);
});
